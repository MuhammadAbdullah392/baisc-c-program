#include<stdio.h>
int main()
{
	int sum=0,x;
	printf("enterd no is");
	scanf("%d",x);
	
	for (1;x=0;x)
	{
		scanf("%d",&x);
		sum=sum+x;
	}
	printf("total sum of entered natural no is is equal=%d",sum);
}
